/** @type {import('tailwindcss').Config} */
export default {
  // Tailwind CSS v4 uses CSS-based configuration via @theme directive
  // This minimal config file exists only for plugin discovery
  // See src/styles/tailwind.css for the actual v4 configuration
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
}
